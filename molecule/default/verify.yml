---
- name: Verify
  hosts: all
  become: true
  gather_facts: false

  tasks:
    - name: Check mvn version
      ansible.builtin.command:
        cmd: /usr/local/bin/mvn --version
      changed_when: false
